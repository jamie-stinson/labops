controller:
  replicas: 1
  resources:
    requests:
      cpu: "25m"
      memory: "256Mi"
      ephemeral-storage: "16Mi"
    limits:
      memory: "256Mi"
      ephemeral-storage: "16Mi"
  pdb:
    enabled: true
    minAvailable: 1
